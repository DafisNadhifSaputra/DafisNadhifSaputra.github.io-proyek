{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
      "config": { 
        "buildCommand": "pyenv global 3.10 && pip install -r requirements.txt" 
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
